---
layout: simplest
---
<input type="text" id="searchBox" onkeyup="Search()" placeholder="Search for names..">

<table id="sourcingGuide">
 <thead>
 <tr>
  <th>Category</th>
  <th>Item</th>
  <th colspan=2>Recommended</th>
  <th colspan=2>Alternative 1</th> 
  <th colspan=2>Alternative 2</th> 
 </tr>
 </thead>
<tbody> 
{% for entry in site.data.sourcing %}
{% if entry.V2 contains 'X' %}

 <tr>
  <td> {{ entry.category }}</td>
  <td> {{ entry.item }} </td>
 
  <td> 
   {% if entry.source1_link |strip != "" %}
    <a href='{{ entry.source1_link }}'> 
   {% endif %} 
   {{ entry.source1 }}
   {% if entry.source1_link |strip != "" %}
    </a>
   {% endif %} 
  </td>
  <td>
   {% if entry.source1_affiliate|strip != "" %}
    <a href='{{ entry.source1_affiliate }}'> [affiliate link] </a>
   {% endif %} 
  </td>
 
 <td> 
   {% if entry.source2_link |strip != "" %}
    <a href='{{ entry.source2_link }}'> 
   {% endif %} 
   {{ entry.source2 }}
   {% if entry.source2_link |strip != "" %}
    </a>
   {% endif %} 
  </td>
  <td>
   {% if entry.source2_affiliate|strip != "" %}
    <a href='{{ entry.source2_affiliate }}'> [affiliate link] </a>
   {% endif %} 
  </td>


<td> 
   {% if entry.source3_link |strip != "" %}
    <a href='{{ entry.source3_link }}'> 
   {% endif %} 
   {{ entry.source3 }}
   {% if entry.source3_link |strip != "" %}
    </a>
   {% endif %} 
  </td>
  <td>
   {% if entry.source3_affiliate|strip != "" %}
    <a href='{{ entry.source3_affiliate }}'> [affiliate link] </a>
   {% endif %} 
  </td>
  
 </tr>
 <tr>
 <td colspan=8 id="comment"> {{ entry.comments }} </td>
 </tr>
{% endif %}
{% endfor %}
 </tbody> 
</table>

<script>
function Search() {
  // Declare variables
  var input, filter, table, tr, td, i, txtValue;
  var show, commentRow;
  input = document.getElementById("searchBox");
  filter = input.value.toUpperCase();
  table = document.getElementById("sourcingGuide");
  tr = table.getElementsByTagName("tr");

  // Loop through all table rows, and hide those who don't match the search query
  for (i = 1; i < tr.length; i=i+2) {  // skip headings, iterate only primary rows, not comments
    show = 0;
    commentRow = (i+1);
    tdMain = tr[i].getElementsByTagName("td")[1];
    if (tdMain) {
      txtValue = tdMain.textContent || tdMain.innerText;
      if (txtValue.toUpperCase().indexOf(filter) > -1) {
       show = 1;
      }
    }
    tdComment = tr[commentRow].getElementsByTagName("td")[0];
    if (tdMain) {
      txtValue = tdMain.textContent || tdMain.innerText;
      if (txtValue.toUpperCase().indexOf(filter) > -1) {
       show = 1;
      }
    }
    if (show == 1) {
        tr[i].style.display = "";
        tr[commentRow].style.display = "";
      } else {
        tr[i].style.display = "none";
        tr[commentRow].style.display = "none";
    }
  }
}
</script>
